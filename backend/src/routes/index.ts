/**
 * Routes Index
 * 
 * Central export point for all route modules.
 * Each route module handles a specific API domain.
 */

import { Router } from 'express'

const router = Router()

// Register route modules here
// Example: router.use('/questions', questionRoutes)

export default router
